{
  "name": "dcc-odysseus-mcp",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "MCP read-only bridge for full DCC or Research Hub-only access",
  "scripts": {
    "start": "node dist/index.js",
    "build": "node ./node_modules/typescript/bin/tsc",
    "dev": "npm run build && node dist/index.js",
    "verify:research": "npm run build && node scripts/verify-research-toolset.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@types/node": "^20.17.19",
    "typescript": "^5.8.2",
    "zod": "^3.24.2"
  }
}
